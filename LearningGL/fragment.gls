#version 460 core

out vec4 fragmentColor;
in vec2 textureCoord;

uniform sampler2D background;
uniform sampler2D face;
uniform float mix_ratio = 0.2;

void main() {
	fragmentColor = mix(texture(background, textureCoord), texture(face, textureCoord), 0.2);
}